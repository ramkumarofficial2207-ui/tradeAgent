# The browser uses same-origin /api routes in production and Vite's local
# proxy in development. API and AI provider secrets belong only in the root
# backend environment; never expose them through VITE_* variables.
